import React from 'react'
import { Text, View } from 'react-native'

export const ScreenStoresList = () => {
  return (
    <View style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', flex: 1 }}>
      <Text style={{ color: "#111" }}>In the pipeline</Text>
    </View>
  )
}
